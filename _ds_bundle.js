// TEDxAmsterdam Design System — browser bundle
// Generated by scripts/build-bundle.js — do not edit manually.
// Components: Accordion, ArrowLink, Badge, Breadcrumb, Button, Checkbox, CheckboxGroup, FormField, Input, Logo, MediaCard, Modal, NavigationBar, Radio, RadioGroup, SectionHeader, Select, SocialLink, Stat, Tag, Tabs, Textarea, Toast
(function () {
  'use strict';
  var __ds_scope = {};
  var __ds_ns = window.TEDxAmsterdamDesignSystem = window.TEDxAmsterdamDesignSystem || {};
  __ds_ns.__errors = [];

// components/core/Accordion.jsx
try { (() => {
function Accordion({ items = [], defaultOpen = 0, style }) {
  const [open, setOpen] = React.useState(defaultOpen);
  return /* @__PURE__ */ React.createElement("div", { style }, items.map((it, i) => {
    const isOpen = open === i;
    const btnId = `accordion-btn-${i}`;
    const panelId = `accordion-panel-${i}`;
    return /* @__PURE__ */ React.createElement("div", { key: i, style: { borderTop: "1px solid var(--border-hairline)" } }, /* @__PURE__ */ React.createElement(
      "button",
      {
        id: btnId,
        "aria-expanded": isOpen,
        "aria-controls": panelId,
        onClick: () => setOpen(isOpen ? -1 : i),
        style: {
          width: "100%",
          background: "none",
          border: "none",
          cursor: "pointer",
          display: "flex",
          justifyContent: "space-between",
          alignItems: "center",
          gap: 24,
          padding: "26px 0",
          textAlign: "left"
        }
      },
      /* @__PURE__ */ React.createElement("span", { style: { font: "700 22px/1.2 var(--font-display)", letterSpacing: "-.01em", textTransform: "uppercase", color: isOpen ? "var(--tedx-red)" : "var(--tedx-white)" } }, it.q),
      /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true", style: { flex: "none", width: 26, height: 26, position: "relative", color: isOpen ? "var(--tedx-red)" : "var(--tedx-white)" } }, /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", top: "50%", left: 0, right: 0, height: 2, background: "currentColor", transform: "translateY(-50%)" } }), /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", left: "50%", top: 0, bottom: 0, width: 2, background: "currentColor", transform: `translateX(-50%) scaleY(${isOpen ? 0 : 1})`, transition: "transform var(--dur) var(--ease-out)" } }))
    ), /* @__PURE__ */ React.createElement(
      "div",
      {
        id: panelId,
        role: "region",
        "aria-labelledby": btnId,
        "aria-hidden": !isOpen,
        style: { maxHeight: isOpen ? 260 : 0, overflow: "hidden", transition: "max-height var(--dur-slow) var(--ease-out)" }
      },
      /* @__PURE__ */ React.createElement("p", { style: { margin: 0, padding: "0 0 26px", font: "var(--text-body)", color: "var(--text-secondary)", maxWidth: 760 } }, it.a)
    ));
  }));
}
  __ds_scope.Accordion = Accordion;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Accordion", error: String((e && e.message) || e) }); }

// components/core/ArrowLink.jsx
try { (() => {
function ArrowLink({ children, href = "#", color = "red", style, ...rest }) {
  const c = color === "white" ? "var(--tedx-white)" : "var(--tedx-red)";
  return /* @__PURE__ */ React.createElement(
    "a",
    {
      href,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        fontFamily: "var(--font-sans)",
        fontWeight: 700,
        fontSize: "var(--fs-tag)",
        lineHeight: 1.2,
        color: c,
        textDecoration: "none",
        transition: "gap var(--dur) var(--ease-out)",
        ...style
      },
      onMouseEnter: (e) => e.currentTarget.style.gap = "12px",
      onMouseLeave: (e) => e.currentTarget.style.gap = "8px",
      ...rest
    },
    children,
    /* @__PURE__ */ React.createElement("svg", { width: "18", height: "9", viewBox: "0 0 18 9", fill: "none", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("path", { d: "M0 4.5h15", stroke: "currentColor", strokeWidth: "1.6" }), /* @__PURE__ */ React.createElement("path", { d: "M11.5 1l4 3.5-4 3.5", stroke: "currentColor", strokeWidth: "1.6", fill: "none" }))
  );
}
  __ds_scope.ArrowLink = ArrowLink;
})(); } catch (e) { __ds_ns.__errors.push({ name: "ArrowLink", error: String((e && e.message) || e) }); }

// components/core/Badge.jsx
try { (() => {
function Badge({ label, color = "red", size = "md", style }) {
  const colors = {
    red: { background: "var(--tedx-red)", color: "var(--tedx-white)" },
    white: { background: "var(--tedx-white)", color: "var(--tedx-black)" },
    muted: { background: "rgba(255,255,255,0.12)", color: "var(--text-secondary)" }
  };
  const fontSize = size === "sm" ? 10 : 12;
  const padding = size === "sm" ? "2px 8px" : "4px 10px";
  return /* @__PURE__ */ React.createElement(
    "span",
    {
      style: {
        display: "inline-block",
        fontFamily: "var(--font-sans)",
        fontWeight: "var(--weight-bold)",
        fontSize,
        lineHeight: 1.4,
        letterSpacing: "var(--ls-wide)",
        padding,
        borderRadius: "var(--radius-full)",
        ...colors[color],
        ...style
      }
    },
    label
  );
}
  __ds_scope.Badge = Badge;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Badge", error: String((e && e.message) || e) }); }

// components/core/Breadcrumb.jsx
try { (() => {
function Breadcrumb({ items = [], ariaLabel = "Breadcrumb", style }) {
  return /* @__PURE__ */ React.createElement("nav", { "aria-label": ariaLabel, style }, /* @__PURE__ */ React.createElement(
    "ol",
    {
      style: {
        display: "flex",
        flexWrap: "wrap",
        alignItems: "center",
        gap: "var(--space-2)",
        listStyle: "none",
        margin: 0,
        padding: 0,
        fontFamily: "var(--font-sans)",
        fontWeight: "var(--weight-bold)",
        fontSize: "var(--fs-tag)",
        letterSpacing: "var(--ls-wide)",
        textTransform: "uppercase"
      }
    },
    items.map((item, i) => {
      const isCurrent = i === items.length - 1;
      return /* @__PURE__ */ React.createElement("li", { key: i, style: { display: "flex", alignItems: "center", gap: "var(--space-2)" } }, !isCurrent && item.href ? /* @__PURE__ */ React.createElement(
        "a",
        {
          href: item.href,
          style: { color: "var(--text-secondary)", textDecoration: "none" }
        },
        item.label
      ) : /* @__PURE__ */ React.createElement(
        "span",
        {
          "aria-current": isCurrent ? "page" : void 0,
          style: { color: isCurrent ? "var(--text-primary)" : "var(--text-secondary)" }
        },
        item.label
      ), !isCurrent && /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true", style: { color: "var(--text-muted)" } }, "\u203A"));
    })
  ));
}
  __ds_scope.Breadcrumb = Breadcrumb;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Breadcrumb", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
const VARIANTS = {
  primary: { background: "var(--tedx-red)", color: "var(--tedx-white)", border: "none" },
  secondary: { background: "var(--tedx-white)", color: "var(--tedx-black)", border: "none" },
  ghost: { background: "transparent", color: "var(--tedx-white)", border: "1.5px solid var(--border-hairline)" }
};
const HOVER_BG = {
  primary: "var(--accent-hover)",
  secondary: "#eaeaea",
  ghost: "rgba(255,255,255,0.08)"
};
const PADS = { sm: "12px 24px", md: "16px 30px", lg: "22px 34px" };
function Button({
  children,
  variant = "primary",
  size = "md",
  arrow = false,
  href,
  type = "button",
  disabled = false,
  onClick,
  style,
  ...rest
}) {
  const [hovered, setHovered] = React.useState(false);
  const [pressed, setPressed] = React.useState(false);
  const isHovered = hovered && !disabled;
  const isPressed = pressed && !disabled;
  const computedStyle = {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 10,
    fontFamily: "var(--font-sans)",
    fontWeight: 700,
    fontSize: size === "lg" ? 16 : 14,
    lineHeight: 1,
    letterSpacing: "0.01em",
    padding: PADS[size],
    borderRadius: "var(--radius-button)",
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.45 : 1,
    textDecoration: "none",
    transition: "background var(--dur) var(--ease-standard), transform var(--dur-fast) var(--ease-standard), opacity var(--dur) var(--ease-standard)",
    WebkitTapHighlightColor: "transparent",
    ...VARIANTS[variant],
    background: isHovered ? HOVER_BG[variant] : VARIANTS[variant].background,
    transform: isPressed ? "scale(0.97)" : "scale(1)",
    ...style
  };
  const Arrow = arrow ? /* @__PURE__ */ React.createElement("svg", { width: "18", height: "9", viewBox: "0 0 18 9", fill: "none", "aria-hidden": "true" }, /* @__PURE__ */ React.createElement("path", { d: "M0 4.5h15", stroke: "currentColor", strokeWidth: "1.6" }), /* @__PURE__ */ React.createElement("path", { d: "M11.5 1l4 3.5-4 3.5", stroke: "currentColor", strokeWidth: "1.6", fill: "none" })) : null;
  const Tag = href ? "a" : "button";
  const tagProps = href ? { href: disabled ? void 0 : href, ...disabled && { "aria-disabled": true, tabIndex: 0 } } : { type, disabled, ...disabled && { "aria-disabled": true } };
  return /* @__PURE__ */ React.createElement(
    Tag,
    {
      ...tagProps,
      onClick,
      style: computedStyle,
      onMouseEnter: () => setHovered(true),
      onMouseLeave: () => {
        setHovered(false);
        setPressed(false);
      },
      onMouseDown: () => {
        if (!disabled) setPressed(true);
      },
      onMouseUp: () => setPressed(false),
      onKeyDown: (e) => {
        if (e.key === "Enter" || e.key === " ") {
          if (!disabled) setPressed(true);
        }
      },
      onKeyUp: (e) => {
        if (e.key === "Enter" || e.key === " ") setPressed(false);
      },
      ...rest
    },
    children,
    Arrow
  );
}
  __ds_scope.Button = Button;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Button", error: String((e && e.message) || e) }); }

// components/core/Checkbox.jsx
try { (() => {
function Checkbox({ label, checked, onChange, disabled = false, id, style }) {
  const checkId = id || `checkbox-${String(label).toLowerCase().replace(/\s+/g, "-")}`;
  return /* @__PURE__ */ React.createElement(
    "label",
    {
      htmlFor: checkId,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "var(--space-3)",
        cursor: disabled ? "not-allowed" : "pointer",
        opacity: disabled ? 0.45 : 1,
        ...style
      }
    },
    /* @__PURE__ */ React.createElement(
      "span",
      {
        style: {
          position: "relative",
          width: 18,
          height: 18,
          flex: "none",
          border: `1.5px solid ${checked ? "var(--tedx-red)" : "var(--border-hairline)"}`,
          borderRadius: 2,
          background: checked ? "var(--tedx-red)" : "transparent",
          transition: "background var(--dur) var(--ease-standard), border-color var(--dur) var(--ease-standard)"
        }
      },
      checked && /* @__PURE__ */ React.createElement(
        "svg",
        {
          "aria-hidden": "true",
          width: "10",
          height: "8",
          viewBox: "0 0 10 8",
          fill: "none",
          style: {
            position: "absolute",
            top: "50%",
            left: "50%",
            transform: "translate(-50%, -50%)"
          }
        },
        /* @__PURE__ */ React.createElement(
          "path",
          {
            d: "M1 4l2.5 2.5L9 1",
            stroke: "white",
            strokeWidth: "1.8",
            strokeLinecap: "round",
            strokeLinejoin: "round"
          }
        )
      ),
      /* @__PURE__ */ React.createElement(
        "input",
        {
          id: checkId,
          type: "checkbox",
          checked,
          onChange: (e) => onChange?.(e.target.checked),
          disabled,
          style: {
            position: "absolute",
            opacity: 0,
            width: "100%",
            height: "100%",
            margin: 0,
            cursor: "inherit"
          }
        }
      )
    ),
    /* @__PURE__ */ React.createElement("span", { style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)" } }, label)
  );
}
  __ds_scope.Checkbox = Checkbox;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Checkbox", error: String((e && e.message) || e) }); }

// components/core/CheckboxGroup.jsx
try { (() => {
function CheckboxGroup({ legend, children, style }) {
  return /* @__PURE__ */ React.createElement(
    "fieldset",
    {
      style: {
        border: "none",
        padding: 0,
        margin: 0,
        display: "flex",
        flexDirection: "column",
        gap: "var(--space-3)",
        ...style
      }
    },
    legend && /* @__PURE__ */ React.createElement(
      "legend",
      {
        style: {
          font: "var(--text-body)",
          fontSize: 16,
          color: "var(--text-primary)",
          opacity: 0.85,
          padding: 0,
          marginBottom: "var(--space-2)"
        }
      },
      legend
    ),
    children
  );
}
  __ds_scope.CheckboxGroup = CheckboxGroup;
})(); } catch (e) { __ds_ns.__errors.push({ name: "CheckboxGroup", error: String((e && e.message) || e) }); }

// components/core/FormField.jsx
try { (() => {
function FormField({ label, htmlFor, hint, error, required = false, children, style }) {
  const errorId = error && htmlFor ? `${htmlFor}-formfield-error` : void 0;
  return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: "var(--space-1)", ...style } }, label && /* @__PURE__ */ React.createElement(
    "label",
    {
      htmlFor,
      style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)", opacity: 0.85 }
    },
    label,
    required && /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true", style: { color: "var(--tedx-red)", marginLeft: 2 } }, "*")
  ), hint && /* @__PURE__ */ React.createElement("span", { style: { font: "var(--text-tag)", color: "var(--text-muted)" } }, hint), children, error && /* @__PURE__ */ React.createElement("span", { id: errorId, "aria-live": "polite", style: { font: "var(--text-tag)", color: "var(--tedx-red)" } }, error));
}
  __ds_scope.FormField = FormField;
})(); } catch (e) { __ds_ns.__errors.push({ name: "FormField", error: String((e && e.message) || e) }); }

// components/core/Input.jsx
try { (() => {
function Input({
  label = "Email",
  type = "email",
  placeholder = "",
  value,
  onChange,
  tone = "dark",
  style,
  ...rest
}) {
  const [focused, setFocused] = React.useState(false);
  const ink = tone === "dark" ? "var(--tedx-white)" : "var(--tedx-black)";
  const rule = tone === "dark" ? "var(--border-hairline)" : "rgba(0,0,0,0.3)";
  return /* @__PURE__ */ React.createElement("label", { style: { display: "flex", flexDirection: "column", gap: 8, ...style } }, /* @__PURE__ */ React.createElement("span", { style: { font: "var(--text-body)", fontSize: 16, color: ink, opacity: 0.85 } }, label), /* @__PURE__ */ React.createElement(
    "input",
    {
      type,
      placeholder,
      value,
      onChange,
      onFocus: () => setFocused(true),
      onBlur: () => setFocused(false),
      style: {
        appearance: "none",
        background: "transparent",
        border: "none",
        borderBottom: `1px solid ${focused ? "var(--tedx-red)" : rule}`,
        padding: "8px 0",
        font: "var(--text-body)",
        color: ink,
        outline: focused ? "2px solid var(--focus-ring)" : "none",
        outlineOffset: "2px",
        transition: "border-color var(--dur) var(--ease-standard), outline var(--dur-fast) var(--ease-standard)"
      },
      ...rest
    }
  ));
}
  __ds_scope.Input = Input;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Input", error: String((e && e.message) || e) }); }

// components/core/Logo.jsx
try { (() => {
function Logo({ tone = "light-on-dark", width = 230, base = "", href, style }) {
  const src = tone === "dark-on-light" ? base + "assets/logo-tedxamsterdam.svg" : base + "assets/logo-tedxamsterdam-white.png";
  const img = /* @__PURE__ */ React.createElement(
    "img",
    {
      src,
      alt: "TEDxAmsterdam",
      style: { width, height: "auto", display: "block" }
    }
  );
  const wrap = { display: "inline-flex", ...style };
  return href ? /* @__PURE__ */ React.createElement("a", { href, style: { ...wrap, textDecoration: "none" } }, img) : /* @__PURE__ */ React.createElement("span", { style: wrap }, img);
}
  __ds_scope.Logo = Logo;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Logo", error: String((e && e.message) || e) }); }

// components/core/MediaCard.jsx
try { (() => {
import { Tag } from "./Tag.jsx";
function MediaCard({
  image,
  category,
  title,
  description,
  date,
  href = "#",
  ratio = "4 / 3",
  style
}) {
  return /* @__PURE__ */ React.createElement(
    "a",
    {
      href,
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 16,
        textDecoration: "none",
        color: "var(--text-primary)",
        ...style
      },
      onMouseEnter: (e) => {
        const img = e.currentTarget.querySelector("[data-img]");
        if (img) img.style.transform = "scale(1.04)";
      },
      onMouseLeave: (e) => {
        const img = e.currentTarget.querySelector("[data-img]");
        if (img) img.style.transform = "scale(1)";
      }
    },
    /* @__PURE__ */ React.createElement("div", { style: { position: "relative", overflow: "hidden", aspectRatio: ratio, background: "var(--grad-tile)" } }, /* @__PURE__ */ React.createElement(
      "div",
      {
        "data-img": true,
        style: {
          position: "absolute",
          inset: 0,
          background: image ? `url(${image}) center / cover no-repeat` : "var(--grad-tile)",
          transition: "transform var(--dur-slow) var(--ease-out)"
        }
      }
    ), category && /* @__PURE__ */ React.createElement(
      "span",
      {
        style: {
          position: "absolute",
          left: 14,
          top: 14,
          font: "var(--text-tag)",
          textTransform: "uppercase",
          letterSpacing: "0.04em",
          color: "var(--tedx-white)",
          textShadow: "0 1px 6px rgba(0,0,0,0.6)"
        }
      },
      category
    )),
    /* @__PURE__ */ React.createElement(
      "h3",
      {
        style: {
          margin: 0,
          font: "700 22px/1.15 var(--font-display)",
          letterSpacing: "var(--ls-display)",
          textTransform: "uppercase"
        }
      },
      title
    ),
    description && /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "var(--text-body)", fontSize: 16, color: "var(--text-muted)" } }, description),
    date && /* @__PURE__ */ React.createElement(Tag, { color: "white", dot: true, style: { textTransform: "none", letterSpacing: 0, fontWeight: 400, fontSize: 16 } }, date)
  );
}
  __ds_scope.MediaCard = MediaCard;
})(); } catch (e) { __ds_ns.__errors.push({ name: "MediaCard", error: String((e && e.message) || e) }); }

// components/core/Modal.jsx
try { (() => {
function Modal({ isOpen, onClose, title, children, size = "md", hideCloseButton = false, style }) {
  const dialogRef = React.useRef(null);
  const titleId = `modal-title-${React.useId()}`;
  const maxWidths = { sm: 480, md: 640, lg: 800 };
  React.useEffect(() => {
    if (!isOpen) return;
    const prev = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    const onKey = (e) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", onKey);
    return () => {
      document.body.style.overflow = prev;
      document.removeEventListener("keydown", onKey);
    };
  }, [isOpen, onClose]);
  React.useEffect(() => {
    if (!isOpen || !dialogRef.current) return;
    const getFirst = () => dialogRef.current?.querySelectorAll(
      'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
    )[0];
    getFirst()?.focus();
    const trap = (e) => {
      if (e.key !== "Tab" || !dialogRef.current) return;
      const focusable = Array.from(dialogRef.current.querySelectorAll(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      ));
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (e.shiftKey) {
        if (document.activeElement === first) {
          e.preventDefault();
          last?.focus();
        }
      } else {
        if (document.activeElement === last) {
          e.preventDefault();
          first?.focus();
        }
      }
    };
    document.addEventListener("keydown", trap);
    return () => document.removeEventListener("keydown", trap);
  }, [isOpen]);
  if (!isOpen) return null;
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      onClick: onClose,
      style: {
        position: "fixed",
        inset: 0,
        background: "rgba(0,0,0,0.8)",
        backdropFilter: "blur(4px)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        padding: "var(--space-6)",
        zIndex: 1e3
      }
    },
    /* @__PURE__ */ React.createElement(
      "div",
      {
        ref: dialogRef,
        role: "dialog",
        "aria-modal": "true",
        "aria-labelledby": title ? titleId : void 0,
        onClick: (e) => e.stopPropagation(),
        style: {
          background: "var(--bg-elevated)",
          borderTop: "2px solid var(--tedx-red)",
          borderRadius: "var(--radius-card)",
          width: "100%",
          maxWidth: maxWidths[size],
          padding: "var(--space-8)",
          position: "relative",
          boxShadow: "var(--shadow-card)",
          ...style
        }
      },
      /* @__PURE__ */ React.createElement(
        "div",
        {
          style: {
            display: "flex",
            justifyContent: "space-between",
            alignItems: "flex-start",
            marginBottom: "var(--space-6)"
          }
        },
        title && /* @__PURE__ */ React.createElement(
          "h2",
          {
            id: titleId,
            style: {
              margin: 0,
              font: "var(--text-h3)",
              color: "var(--text-primary)",
              textTransform: "uppercase",
              letterSpacing: "var(--ls-display)"
            }
          },
          title
        ),
        !hideCloseButton && /* @__PURE__ */ React.createElement(
          "button",
          {
            onClick: onClose,
            "aria-label": "Close dialog",
            style: {
              background: "none",
              border: "none",
              cursor: "pointer",
              color: "var(--text-secondary)",
              fontSize: 24,
              lineHeight: 1,
              padding: "var(--space-1)",
              marginLeft: "var(--space-4)",
              flex: "none"
            }
          },
          "\xD7"
        )
      ),
      /* @__PURE__ */ React.createElement("div", { style: { color: "var(--text-secondary)", font: "var(--text-body)", fontSize: 16 } }, children)
    )
  );
}
  __ds_scope.Modal = Modal;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Modal", error: String((e && e.message) || e) }); }

// components/core/NavigationBar.jsx
try { (() => {
function NavigationBar({
  links = [],
  ctaLabel,
  ctaHref,
  logoHref = "/",
  activePath,
  base = "",
  skipTarget = "#main-content",
  style
}) {
  const [menuOpen, setMenuOpen] = React.useState(false);
  const uid = React.useId();
  const menuId = `nav-mobile-menu-${uid}`;
  const hamburgerRef = React.useRef(null);
  React.useEffect(() => {
    if (!menuOpen) return;
    const onKey = (e) => {
      if (e.key === "Escape") {
        setMenuOpen(false);
        hamburgerRef.current?.focus();
      }
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [menuOpen]);
  const logoSrc = base + "assets/logo-tedxamsterdam-white.png";
  const linkStyle = (href) => ({
    fontFamily: "var(--font-sans)",
    fontWeight: "var(--weight-bold)",
    fontSize: "var(--fs-tag)",
    letterSpacing: "var(--ls-wide)",
    textTransform: "uppercase",
    color: activePath === href ? "var(--text-accent)" : "var(--text-primary)",
    textDecoration: "none"
  });
  const ctaStyle = {
    display: "inline-flex",
    alignItems: "center",
    fontFamily: "var(--font-sans)",
    fontWeight: "var(--weight-bold)",
    fontSize: 14,
    padding: "12px 24px",
    background: "var(--tedx-red)",
    color: "var(--tedx-white)",
    borderRadius: "var(--radius-button)",
    textDecoration: "none",
    flex: "none",
    lineHeight: 1
  };
  const scopedCss = `
    [data-nav-uid="${uid}"] .nav-skip-link {
      position: absolute; top: -40px; left: 16px; z-index: 200;
      background: var(--bg-elevated); color: var(--text-primary);
      padding: var(--space-3) var(--space-6); border-radius: var(--radius-button);
      font-family: var(--font-sans); font-weight: 700; font-size: 14px;
      text-decoration: none;
      transition: top var(--dur-fast) var(--ease-standard);
    }
    [data-nav-uid="${uid}"] .nav-skip-link:focus { top: 8px; }
    [data-nav-uid="${uid}"] .nav-desktop {
      display: flex; align-items: center; gap: var(--space-8); margin-left: auto;
    }
    [data-nav-uid="${uid}"] .nav-hamburger { display: none; margin-left: auto; }
    @media (max-width: 767px) {
      [data-nav-uid="${uid}"] .nav-desktop { display: none; }
      [data-nav-uid="${uid}"] .nav-hamburger { display: flex; align-items: center; }
    }
  `;
  return /* @__PURE__ */ React.createElement(
    "header",
    {
      "data-nav-uid": uid,
      style: {
        position: "sticky",
        top: 0,
        zIndex: 100,
        width: "100%",
        background: "var(--bg-base)",
        borderBottom: "1px solid var(--border-hairline)",
        boxSizing: "border-box",
        ...style
      }
    },
    /* @__PURE__ */ React.createElement("style", null, scopedCss),
    /* @__PURE__ */ React.createElement("a", { href: skipTarget, className: "nav-skip-link" }, "Skip to main content"),
    /* @__PURE__ */ React.createElement("div", { style: { height: "var(--header-height)", display: "flex", alignItems: "center", padding: "0 var(--gutter)", gap: "var(--space-8)" } }, /* @__PURE__ */ React.createElement("a", { href: logoHref, style: { display: "inline-flex", textDecoration: "none", flex: "none" } }, /* @__PURE__ */ React.createElement("img", { src: logoSrc, alt: "TEDxAmsterdam", style: { width: 160, height: "auto", display: "block" } })), /* @__PURE__ */ React.createElement("div", { className: "nav-desktop" }, /* @__PURE__ */ React.createElement("nav", { "aria-label": "Main navigation", style: { display: "flex", alignItems: "center", gap: "var(--space-8)" } }, links.map((link) => /* @__PURE__ */ React.createElement(
      "a",
      {
        key: link.href,
        href: link.href,
        "aria-current": activePath === link.href ? "page" : void 0,
        style: linkStyle(link.href)
      },
      link.label
    ))), ctaHref && ctaLabel && /* @__PURE__ */ React.createElement("a", { href: ctaHref, style: ctaStyle }, ctaLabel)), /* @__PURE__ */ React.createElement(
      "button",
      {
        ref: hamburgerRef,
        type: "button",
        className: "nav-hamburger",
        "aria-expanded": menuOpen,
        "aria-controls": menuId,
        "aria-label": menuOpen ? "Close menu" : "Open menu",
        onClick: () => setMenuOpen(!menuOpen),
        style: { background: "none", border: "none", cursor: "pointer", color: "var(--text-primary)", padding: "var(--space-2)", display: "flex", alignItems: "center" }
      },
      /* @__PURE__ */ React.createElement("svg", { "aria-hidden": "true", width: "24", height: "18", viewBox: "0 0 24 18", fill: "none" }, menuOpen ? /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("line", { x1: "2", y1: "2", x2: "22", y2: "16", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" }), /* @__PURE__ */ React.createElement("line", { x1: "22", y1: "2", x2: "2", y2: "16", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" })) : /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("line", { x1: "0", y1: "2", x2: "24", y2: "2", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" }), /* @__PURE__ */ React.createElement("line", { x1: "0", y1: "9", x2: "24", y2: "9", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" }), /* @__PURE__ */ React.createElement("line", { x1: "0", y1: "16", x2: "24", y2: "16", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" })))
    )),
    menuOpen && /* @__PURE__ */ React.createElement("div", { id: menuId, style: { background: "var(--bg-base)", borderBottom: "1px solid var(--border-hairline)", padding: "var(--space-6) var(--gutter)", display: "flex", flexDirection: "column", gap: "var(--space-4)" } }, /* @__PURE__ */ React.createElement("nav", { "aria-label": "Main navigation" }, links.map((link) => /* @__PURE__ */ React.createElement(
      "a",
      {
        key: link.href,
        href: link.href,
        onClick: () => setMenuOpen(false),
        "aria-current": activePath === link.href ? "page" : void 0,
        style: { ...linkStyle(link.href), display: "block", padding: "var(--space-2) 0" }
      },
      link.label
    ))), ctaHref && ctaLabel && /* @__PURE__ */ React.createElement("a", { href: ctaHref, onClick: () => setMenuOpen(false), style: { ...ctaStyle, alignSelf: "flex-start" } }, ctaLabel))
  );
}
  __ds_scope.NavigationBar = NavigationBar;
})(); } catch (e) { __ds_ns.__errors.push({ name: "NavigationBar", error: String((e && e.message) || e) }); }

// components/core/Radio.jsx
try { (() => {
function Radio({ legend, options = [], value, onChange, name, disabled = false, style }) {
  return /* @__PURE__ */ React.createElement(
    "fieldset",
    {
      style: {
        border: "none",
        padding: 0,
        margin: 0,
        display: "flex",
        flexDirection: "column",
        gap: "var(--space-3)",
        ...style
      }
    },
    legend && /* @__PURE__ */ React.createElement(
      "legend",
      {
        style: {
          font: "var(--text-body)",
          fontSize: 16,
          color: "var(--text-primary)",
          opacity: 0.85,
          padding: 0,
          marginBottom: "var(--space-2)"
        }
      },
      legend
    ),
    options.map((opt) => {
      const isSelected = value === opt.value;
      const radioId = `${name}-${opt.value}`;
      return /* @__PURE__ */ React.createElement(
        "label",
        {
          key: opt.value,
          htmlFor: radioId,
          style: {
            display: "inline-flex",
            alignItems: "center",
            gap: "var(--space-3)",
            cursor: disabled ? "not-allowed" : "pointer",
            opacity: disabled ? 0.45 : 1
          }
        },
        /* @__PURE__ */ React.createElement(
          "span",
          {
            style: {
              position: "relative",
              width: 18,
              height: 18,
              flex: "none",
              border: `1.5px solid ${isSelected ? "var(--tedx-red)" : "var(--border-hairline)"}`,
              borderRadius: "var(--radius-full)",
              background: "transparent",
              transition: "border-color var(--dur) var(--ease-standard)"
            }
          },
          isSelected && /* @__PURE__ */ React.createElement(
            "span",
            {
              style: {
                position: "absolute",
                top: "50%",
                left: "50%",
                transform: "translate(-50%, -50%)",
                width: 8,
                height: 8,
                borderRadius: "var(--radius-full)",
                background: "var(--tedx-red)"
              }
            }
          ),
          /* @__PURE__ */ React.createElement(
            "input",
            {
              id: radioId,
              type: "radio",
              name,
              value: opt.value,
              checked: isSelected,
              onChange: () => onChange?.(opt.value),
              disabled,
              style: {
                position: "absolute",
                opacity: 0,
                width: "100%",
                height: "100%",
                margin: 0,
                cursor: "inherit"
              }
            }
          )
        ),
        /* @__PURE__ */ React.createElement("span", { style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)" } }, opt.label)
      );
    })
  );
}
  __ds_scope.Radio = Radio;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Radio", error: String((e && e.message) || e) }); }

// components/core/RadioGroup.jsx
try { (() => {
function RadioGroupRoot({ name, legend, value, onChange, disabled = false, children, style }) {
  return /* @__PURE__ */ React.createElement(
    "fieldset",
    {
      style: {
        border: "none",
        padding: 0,
        margin: 0,
        display: "flex",
        flexDirection: "column",
        gap: "var(--space-3)",
        ...style
      }
    },
    legend && /* @__PURE__ */ React.createElement(
      "legend",
      {
        style: {
          font: "var(--text-body)",
          fontSize: 16,
          color: "var(--text-primary)",
          opacity: 0.85,
          padding: 0,
          marginBottom: "var(--space-2)"
        }
      },
      legend
    ),
    React.Children.map(
      children,
      (child) => React.isValidElement(child) ? React.cloneElement(child, { _name: name, _groupValue: value, _onChange: onChange, _disabled: disabled }) : child
    )
  );
}
function RadioOption({ value, label, _name, _groupValue, _onChange, _disabled }) {
  const id = `${_name}-${value}`;
  const isSelected = _groupValue === value;
  return /* @__PURE__ */ React.createElement(
    "label",
    {
      htmlFor: id,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "var(--space-3)",
        cursor: _disabled ? "not-allowed" : "pointer",
        opacity: _disabled ? 0.45 : 1
      }
    },
    /* @__PURE__ */ React.createElement(
      "span",
      {
        style: {
          position: "relative",
          width: 18,
          height: 18,
          flex: "none",
          border: `1.5px solid ${isSelected ? "var(--tedx-red)" : "var(--border-hairline)"}`,
          borderRadius: "var(--radius-full)",
          background: "transparent",
          transition: "border-color var(--dur) var(--ease-standard)"
        }
      },
      isSelected && /* @__PURE__ */ React.createElement("span", { style: { position: "absolute", top: "50%", left: "50%", transform: "translate(-50%,-50%)", width: 8, height: 8, borderRadius: "var(--radius-full)", background: "var(--tedx-red)" } }),
      /* @__PURE__ */ React.createElement(
        "input",
        {
          id,
          type: "radio",
          name: _name,
          value,
          checked: isSelected,
          onChange: () => _onChange?.(value),
          disabled: _disabled,
          style: { position: "absolute", opacity: 0, width: "100%", height: "100%", margin: 0, cursor: "inherit" }
        }
      )
    ),
    /* @__PURE__ */ React.createElement("span", { style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)" } }, label)
  );
}
RadioGroupRoot.Option = RadioOption;
const RadioGroup = RadioGroupRoot;
  __ds_scope.RadioGroup = RadioGroup;
})(); } catch (e) { __ds_ns.__errors.push({ name: "RadioGroup", error: String((e && e.message) || e) }); }

// components/core/SectionHeader.jsx
try { (() => {
import { Tag } from "./Tag.jsx";
import { ArrowLink } from "./ArrowLink.jsx";
function SectionHeader({
  eyebrow,
  title,
  description,
  link,
  linkHref = "#",
  align = "left",
  style
}) {
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 20,
        alignItems: align === "center" ? "center" : "flex-start",
        textAlign: align,
        maxWidth: 540,
        ...style
      }
    },
    eyebrow && /* @__PURE__ */ React.createElement(Tag, null, eyebrow),
    /* @__PURE__ */ React.createElement(
      "h2",
      {
        style: {
          margin: 0,
          font: "var(--text-h2)",
          letterSpacing: "var(--ls-display)",
          color: "var(--text-primary)"
        }
      },
      title
    ),
    description && /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "var(--text-body)", color: "var(--text-secondary)" } }, description),
    link && /* @__PURE__ */ React.createElement("div", { style: { marginTop: 4 } }, /* @__PURE__ */ React.createElement(ArrowLink, { href: linkHref }, link))
  );
}
  __ds_scope.SectionHeader = SectionHeader;
})(); } catch (e) { __ds_ns.__errors.push({ name: "SectionHeader", error: String((e && e.message) || e) }); }

// components/core/Select.jsx
try { (() => {
function Select({
  label,
  options = [],
  value,
  onChange,
  placeholder = "Select\u2026",
  disabled = false,
  error,
  id,
  style,
  ...rest
}) {
  const [focused, setFocused] = React.useState(false);
  const selectId = id || `select-${String(label).toLowerCase().replace(/\s+/g, "-")}`;
  const errorId = error ? `${selectId}-error` : void 0;
  return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: "var(--space-2)", ...style } }, label && /* @__PURE__ */ React.createElement(
    "label",
    {
      htmlFor: selectId,
      style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)", opacity: 0.85 }
    },
    label
  ), /* @__PURE__ */ React.createElement("div", { style: { position: "relative" } }, /* @__PURE__ */ React.createElement(
    "select",
    {
      id: selectId,
      value: value ?? "",
      onChange: (e) => onChange?.(e.target.value),
      disabled,
      "aria-invalid": !!error,
      "aria-describedby": errorId,
      onFocus: () => setFocused(true),
      onBlur: () => setFocused(false),
      style: {
        appearance: "none",
        width: "100%",
        background: "transparent",
        border: "none",
        borderBottom: `1px solid ${error ? "var(--tedx-red)" : focused ? "var(--tedx-red)" : "var(--border-hairline)"}`,
        paddingTop: "var(--space-2)",
        paddingBottom: "var(--space-2)",
        paddingRight: "var(--space-8)",
        paddingLeft: 0,
        font: "var(--text-body)",
        fontSize: 16,
        color: value ? "var(--text-primary)" : "var(--text-secondary)",
        cursor: disabled ? "not-allowed" : "pointer",
        opacity: disabled ? 0.45 : 1,
        outline: focused ? "2px solid var(--focus-ring)" : "none",
        outlineOffset: "2px",
        transition: "border-color var(--dur) var(--ease-standard)"
      },
      ...rest
    },
    placeholder && /* @__PURE__ */ React.createElement("option", { value: "", disabled: true, hidden: true }, placeholder),
    options.map((opt) => /* @__PURE__ */ React.createElement("option", { key: opt.value, value: opt.value }, opt.label))
  ), /* @__PURE__ */ React.createElement(
    "svg",
    {
      "aria-hidden": "true",
      width: "12",
      height: "7",
      viewBox: "0 0 12 7",
      fill: "none",
      style: {
        position: "absolute",
        right: 0,
        top: "50%",
        transform: "translateY(-50%)",
        pointerEvents: "none",
        color: "var(--text-secondary)"
      }
    },
    /* @__PURE__ */ React.createElement("path", { d: "M1 1l5 5 5-5", stroke: "currentColor", strokeWidth: "1.6", strokeLinecap: "round" })
  )), error && /* @__PURE__ */ React.createElement("span", { id: errorId, style: { font: "var(--text-tag)", color: "var(--tedx-red)" } }, error));
}
  __ds_scope.Select = Select;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Select", error: String((e && e.message) || e) }); }

// components/core/SocialLink.jsx
try { (() => {
const ICONS = {
  linkedin: "assets/icon-linkedin.svg",
  instagram: "assets/icon-instagram.svg",
  youtube: "assets/icon-youtube.svg"
};
function SocialLink({ network, label, href = "#", base = "", style, ...rest }) {
  const src = base + (ICONS[network] || ICONS.linkedin);
  return /* @__PURE__ */ React.createElement(
    "a",
    {
      href,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 12,
        textDecoration: "none",
        font: "var(--text-body)",
        color: "var(--text-primary)",
        ...style
      },
      onMouseEnter: (e) => e.currentTarget.style.color = "var(--tedx-red)",
      onMouseLeave: (e) => e.currentTarget.style.color = "var(--text-primary)",
      ...rest
    },
    /* @__PURE__ */ React.createElement(
      "img",
      {
        src,
        alt: "",
        "aria-hidden": "true",
        style: { width: 22, height: 22, objectFit: "contain", filter: "brightness(0) invert(1)" }
      }
    ),
    label || network
  );
}
  __ds_scope.SocialLink = SocialLink;
})(); } catch (e) { __ds_ns.__errors.push({ name: "SocialLink", error: String((e && e.message) || e) }); }

// components/core/Stat.jsx
try { (() => {
function Stat({ value, label, size = "lg", style }) {
  const fs = size === "xl" ? 140 : size === "lg" ? 80 : 56;
  return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", alignItems: "flex-start", gap: 28, ...style } }, /* @__PURE__ */ React.createElement(
    "span",
    {
      style: {
        font: `700 ${fs}px/1 var(--font-display)`,
        letterSpacing: "var(--ls-display)",
        color: "var(--text-primary)"
      }
    },
    value
  ), label && /* @__PURE__ */ React.createElement(
    "span",
    {
      style: {
        font: "var(--text-body)",
        color: "var(--text-secondary)",
        maxWidth: 170,
        paddingTop: size === "xl" ? 24 : 14
      }
    },
    label
  ));
}
  __ds_scope.Stat = Stat;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Stat", error: String((e && e.message) || e) }); }

// components/core/Tag.jsx
try { (() => {
function Tag({ children, color = "red", dot = false, style, ...rest }) {
  const colors = {
    red: "var(--tedx-red)",
    white: "var(--tedx-white)",
    muted: "var(--tedx-gray-400)"
  };
  return /* @__PURE__ */ React.createElement(
    "span",
    {
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 8,
        fontFamily: "var(--font-sans)",
        fontWeight: 700,
        fontSize: "var(--fs-tag)",
        lineHeight: 1.2,
        textTransform: "uppercase",
        letterSpacing: "0.04em",
        color: colors[color],
        ...style
      },
      ...rest
    },
    dot && /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": "true",
        style: {
          width: 8,
          height: 8,
          borderRadius: "50%",
          background: "var(--tedx-red)",
          flex: "none"
        }
      }
    ),
    children
  );
}
  __ds_scope.Tag = Tag;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Tag", error: String((e && e.message) || e) }); }

// components/core/Tabs.jsx
try { (() => {
function Tabs({ tabs = [], defaultIndex = 0, selectedIndex, onTabChange, style }) {
  const [internalActive, setInternalActive] = React.useState(defaultIndex);
  const uid = React.useId();
  const tabRefs = React.useRef([]);
  const isControlled = selectedIndex !== void 0;
  const active = isControlled ? selectedIndex : internalActive;
  const setActive = (i) => {
    if (!isControlled) setInternalActive(i);
    onTabChange?.(i);
  };
  const onKeyDown = (e, i) => {
    if (e.key === "ArrowRight") {
      const next = (i + 1) % tabs.length;
      setActive(next);
      tabRefs.current[next]?.focus();
    } else if (e.key === "ArrowLeft") {
      const prev = (i - 1 + tabs.length) % tabs.length;
      setActive(prev);
      tabRefs.current[prev]?.focus();
    }
  };
  return /* @__PURE__ */ React.createElement("div", { style }, /* @__PURE__ */ React.createElement(
    "div",
    {
      role: "tablist",
      style: {
        display: "flex",
        borderBottom: "1px solid var(--border-hairline)",
        marginBottom: "var(--space-6)"
      }
    },
    tabs.map((tab, i) => /* @__PURE__ */ React.createElement(
      "button",
      {
        key: i,
        ref: (el) => tabRefs.current[i] = el,
        role: "tab",
        "aria-selected": active === i,
        "aria-controls": `tabpanel-${uid}-${i}`,
        id: `tab-btn-${uid}-${i}`,
        tabIndex: active === i ? 0 : -1,
        onClick: () => setActive(i),
        onKeyDown: (e) => onKeyDown(e, i),
        style: {
          background: "none",
          border: "none",
          borderBottom: `2px solid ${active === i ? "var(--tedx-red)" : "transparent"}`,
          marginBottom: -1,
          /* overlap the tablist hairline border */
          cursor: "pointer",
          padding: "var(--space-3) var(--space-6)",
          fontFamily: "var(--font-sans)",
          fontWeight: "var(--weight-bold)",
          fontSize: "var(--fs-tag)",
          letterSpacing: "var(--ls-wide)",
          textTransform: "uppercase",
          color: active === i ? "var(--text-primary)" : "var(--text-secondary)",
          transition: "color var(--dur) var(--ease-standard), border-color var(--dur) var(--ease-standard)"
        }
      },
      tab.label
    ))
  ), tabs.map((tab, i) => /* @__PURE__ */ React.createElement(
    "div",
    {
      key: i,
      id: `tabpanel-${uid}-${i}`,
      role: "tabpanel",
      "aria-labelledby": `tab-btn-${uid}-${i}`,
      style: { display: active === i ? "block" : "none" }
    },
    tab.content
  )));
}
  __ds_scope.Tabs = Tabs;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Tabs", error: String((e && e.message) || e) }); }

// components/core/Textarea.jsx
try { (() => {
function Textarea({
  label,
  value,
  onChange,
  placeholder,
  rows = 4,
  disabled = false,
  error,
  id,
  style,
  ...rest
}) {
  const [focused, setFocused] = React.useState(false);
  const taId = id || `textarea-${String(label).toLowerCase().replace(/\s+/g, "-")}`;
  const errorId = error ? `${taId}-error` : void 0;
  return /* @__PURE__ */ React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: "var(--space-2)", ...style } }, label && /* @__PURE__ */ React.createElement(
    "label",
    {
      htmlFor: taId,
      style: { font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)", opacity: 0.85 }
    },
    label
  ), /* @__PURE__ */ React.createElement(
    "textarea",
    {
      id: taId,
      value,
      onChange: (e) => onChange?.(e.target.value),
      placeholder,
      rows,
      disabled,
      "aria-invalid": !!error,
      "aria-describedby": errorId,
      onFocus: () => setFocused(true),
      onBlur: () => setFocused(false),
      style: {
        appearance: "none",
        background: "transparent",
        border: "none",
        borderBottom: `1px solid ${error ? "var(--tedx-red)" : focused ? "var(--tedx-red)" : "var(--border-hairline)"}`,
        padding: "var(--space-2) 0",
        font: "var(--text-body)",
        fontSize: 16,
        color: "var(--text-primary)",
        resize: "vertical",
        cursor: disabled ? "not-allowed" : "auto",
        opacity: disabled ? 0.45 : 1,
        outline: focused ? "2px solid var(--focus-ring)" : "none",
        outlineOffset: "2px",
        transition: "border-color var(--dur) var(--ease-standard)",
        fontFamily: "var(--font-sans)"
      },
      ...rest
    }
  ), error && /* @__PURE__ */ React.createElement("span", { id: errorId, style: { font: "var(--text-tag)", color: "var(--tedx-red)" } }, error));
}
  __ds_scope.Textarea = Textarea;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Textarea", error: String((e && e.message) || e) }); }

// components/core/Toast.jsx
try { (() => {
function Toast({ message, type = "info", duration = 4e3, onDismiss, style }) {
  const [visible, setVisible] = React.useState(false);
  const onDismissRef = React.useRef(onDismiss);
  React.useEffect(() => {
    onDismissRef.current = onDismiss;
  });
  React.useEffect(() => {
    const enter = setTimeout(() => setVisible(true), 16);
    if (duration > 0) {
      const dismiss = setTimeout(() => {
        setVisible(false);
        setTimeout(() => onDismissRef.current?.(), 300);
      }, duration);
      return () => {
        clearTimeout(enter);
        clearTimeout(dismiss);
      };
    }
    return () => clearTimeout(enter);
  }, [duration]);
  const isUrgent = type === "error" || type === "warning";
  const accentColor = isUrgent ? "var(--tedx-red)" : "var(--tedx-white)";
  return /* @__PURE__ */ React.createElement(
    "div",
    {
      role: isUrgent ? "alert" : "status",
      "aria-live": isUrgent ? "assertive" : "polite",
      "aria-atomic": "true",
      style: {
        position: "fixed",
        bottom: "var(--space-6)",
        right: "var(--space-6)",
        maxWidth: 360,
        background: "var(--bg-elevated)",
        borderLeft: `3px solid ${accentColor}`,
        borderRadius: "var(--radius-card)",
        padding: "var(--space-4) var(--space-6)",
        boxShadow: "var(--shadow-card)",
        display: "flex",
        alignItems: "flex-start",
        gap: "var(--space-4)",
        zIndex: 1100,
        transform: visible ? "translateX(0)" : "translateX(calc(100% + var(--space-6)))",
        opacity: visible ? 1 : 0,
        transition: "transform var(--dur-slow) var(--ease-out), opacity var(--dur) var(--ease-standard)",
        ...style
      }
    },
    /* @__PURE__ */ React.createElement("p", { style: { margin: 0, font: "var(--text-body)", fontSize: 16, color: "var(--text-primary)", flex: 1 } }, message),
    /* @__PURE__ */ React.createElement(
      "button",
      {
        onClick: () => {
          setVisible(false);
          setTimeout(() => onDismissRef.current?.(), 300);
        },
        "aria-label": "Dismiss notification",
        style: {
          background: "none",
          border: "none",
          cursor: "pointer",
          color: "var(--text-muted)",
          fontSize: 18,
          lineHeight: 1,
          padding: 0,
          flex: "none"
        }
      },
      "\xD7"
    )
  );
}
  __ds_scope.Toast = Toast;
})(); } catch (e) { __ds_ns.__errors.push({ name: "Toast", error: String((e && e.message) || e) }); }

__ds_ns.Accordion = __ds_scope.Accordion;
__ds_ns.ArrowLink = __ds_scope.ArrowLink;
__ds_ns.Badge = __ds_scope.Badge;
__ds_ns.Breadcrumb = __ds_scope.Breadcrumb;
__ds_ns.Button = __ds_scope.Button;
__ds_ns.Checkbox = __ds_scope.Checkbox;
__ds_ns.CheckboxGroup = __ds_scope.CheckboxGroup;
__ds_ns.FormField = __ds_scope.FormField;
__ds_ns.Input = __ds_scope.Input;
__ds_ns.Logo = __ds_scope.Logo;
__ds_ns.MediaCard = __ds_scope.MediaCard;
__ds_ns.Modal = __ds_scope.Modal;
__ds_ns.NavigationBar = __ds_scope.NavigationBar;
__ds_ns.Radio = __ds_scope.Radio;
__ds_ns.RadioGroup = __ds_scope.RadioGroup;
__ds_ns.SectionHeader = __ds_scope.SectionHeader;
__ds_ns.Select = __ds_scope.Select;
__ds_ns.SocialLink = __ds_scope.SocialLink;
__ds_ns.Stat = __ds_scope.Stat;
__ds_ns.Tag = __ds_scope.Tag;
__ds_ns.Tabs = __ds_scope.Tabs;
__ds_ns.Textarea = __ds_scope.Textarea;
__ds_ns.Toast = __ds_scope.Toast;
})();