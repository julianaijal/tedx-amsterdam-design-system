Oversized Helvetica-Bold figure with a supporting caption; for impact counters.

```jsx
<Stat value="2009" label="events have been organised since" />
<Stat value="20K" size="md" label="people over the last 3 years" />
```

Sizes: `md` 56px, `lg` 80px, `xl` 140px. Stack two with a hairline divider between.
