Minimal underline form field (newsletter / sign-up). Rule turns red on focus.

```jsx
<Input label="Email" type="email" />
<Input label="Email" tone="light" />
```

`tone="dark"` (default) for black surfaces, `"light"` on white.
